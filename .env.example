# Copy this file to .env.local and fill values from:
# Supabase Dashboard -> Project Settings -> API
VITE_SUPABASE_URL=https://hboavkngruumpdunbhvw.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Optional. Origin of the deployed "Super Admin Portal" app, which hosts
# pharmacy registration (#branch) and the super-admin console (#admin).
# Leave unset in local development: the home page falls back to the current
# hostname on port 8444, where that app's dev server runs.
# VITE_ADMIN_PORTAL_URL=https://admin.pharmsync.rw
